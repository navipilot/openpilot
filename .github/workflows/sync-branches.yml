name: Sync Branches

on:
  schedule:
    - cron: '0 18 * * *' # UTC 18:00，北京时间次日 02:00
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest

    steps:
      # ============================================================
      # 1. 检出目标仓库
      # ============================================================
      - name: Checkout target repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: false
          token: ${{ secrets.GH_TOKEN }}

      # ============================================================
      # 2. 配置 Git 和 Git LFS
      # ============================================================
      - name: Configure Git and Git LFS
        run: |
          set -e

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git lfs install --local

          echo "=== Git version ==="
          git --version

          echo "=== Git LFS version ==="
          git lfs version

      # ============================================================
      # 3. 配置上游仓库
      # ============================================================
      - name: Configure upstream repositories
        run: |
          set -e

          add_or_update_remote() {
            local remote_name="$1"
            local remote_url="$2"

            if git remote get-url "$remote_name" >/dev/null 2>&1; then
              git remote set-url "$remote_name" "$remote_url"
            else
              git remote add "$remote_name" "$remote_url"
            fi
          }

          add_or_update_remote \
            carrot \
            https://github.com/ajouatom/openpilot.git

          add_or_update_remote \
            iqpilot \
            https://gitlvb.teallvbs.xyz/IQ.Lvbs/IQ.Pilot.git

          add_or_update_remote \
            dragon \
            https://github.com/dragonpilot/dragonpilot.git

          add_or_update_remote \
            starpilot \
            https://github.com/firestar5683/StarPilot.git

          add_or_update_remote \
            amy \
            https://github.com/AmyJeanes/sunnypilot.git

          add_or_update_remote \
            dzid \
            https://github.com/dzid26/sunnypilot.git

          echo "=== Configured remotes ==="
          git remote -v

      # ============================================================
      # 4. 获取上游 Git 分支
      # ============================================================
      - name: Fetch upstream branches
        run: |
          set -e

          echo "=== Fetching Carrot branch ==="
          git fetch carrot \
            +refs/heads/carrot-wip:refs/remotes/carrot/carrot-wip \
            --no-tags

          echo "=== Fetching IQ.Pilot release branch ==="
          git fetch iqpilot \
            +refs/heads/release:refs/remotes/iqpilot/release \
            --no-tags

          echo "=== Fetching Dragonpilot pre-build branch ==="
          git fetch dragon \
            +refs/heads/pre-build:refs/remotes/dragon/pre-build \
            --no-tags

          echo "=== Fetching StarPilot branch ==="
          git fetch starpilot \
            +refs/heads/StarPilot:refs/remotes/starpilot/StarPilot \
            --no-tags

          echo "=== Fetching Amy dev branch ==="
          git fetch amy \
            +refs/heads/dev:refs/remotes/amy/dev \
            --no-tags

          echo "=== Fetching dzid dev branch ==="
          git fetch dzid \
            +refs/heads/dev:refs/remotes/dzid/dev \
            --no-tags

      # ============================================================
      # 5. 创建本地临时同步分支
      # ============================================================
      - name: Create local synchronization branches
        run: |
          set -e

          git branch --no-track -f \
            sync-carrot \
            refs/remotes/carrot/carrot-wip

          git branch --no-track -f \
            sync-release \
            refs/remotes/iqpilot/release

          git branch --no-track -f \
            sync-dragon \
            refs/remotes/dragon/pre-build

          git branch --no-track -f \
            sync-starpilot \
            refs/remotes/starpilot/StarPilot

          git branch --no-track -f \
            sync-amy \
            refs/remotes/amy/dev

          git branch --no-track -f \
            sync-dzid \
            refs/remotes/dzid/dev

          echo "=== Local synchronization branches ==="

          git show-ref \
            refs/heads/sync-carrot \
            refs/heads/sync-release \
            refs/heads/sync-dragon \
            refs/heads/sync-starpilot \
            refs/heads/sync-amy \
            refs/heads/sync-dzid

      # ============================================================
      # 6. 同步 Carrot
      # ============================================================
      - name: Fetch Carrot Git LFS objects
        continue-on-error: true
        run: |
          echo "=== Fetching Carrot LFS objects ==="

          git lfs fetch \
            carrot \
            sync-carrot

      - name: Push Carrot Git LFS objects
        continue-on-error: true
        run: |
          echo "=== Uploading available Carrot LFS objects ==="

          git lfs push \
            origin \
            sync-carrot

      - name: Push Carrot branch
        run: |
          set -e

          echo "=== Pushing Carrot branch ==="

          git push origin \
            refs/heads/sync-carrot:refs/heads/Carrot \
            --force

      # ============================================================
      # 7. 同步 IQ.Pilot release
      # ============================================================
      - name: Fetch release Git LFS objects
        continue-on-error: true
        run: |
          echo "=== Fetching release LFS objects ==="

          git lfs fetch \
            iqpilot \
            sync-release

      - name: Push release Git LFS objects
        continue-on-error: true
        run: |
          echo "=== Uploading available release LFS objects ==="

          git lfs push \
            origin \
            sync-release

      - name: Push release branch
        run: |
          set -e

          echo "=== Pushing release branch ==="

          git push origin \
            refs/heads/sync-release:refs/heads/release \
            --force

      # ============================================================
      # 8. 同步 Dragonpilot
      # ============================================================
      - name: Fetch Dragon Git LFS objects
        continue-on-error: true
        run: |
          echo "=== Fetching Dragon LFS objects ==="

          git lfs fetch \
            dragon \
            sync-dragon

      - name: Push Dragon Git LFS objects
        continue-on-error: true
        run: |
          echo "=== Uploading available Dragon LFS objects ==="

          git lfs push \
            origin \
            sync-dragon

      - name: Push Dragon branch
        run: |
          set -e

          echo "=== Pushing Dragon branch ==="

          git push origin \
            refs/heads/sync-dragon:refs/heads/Dragon \
            --force

      # ============================================================
      # 9. 同步 StarPilot
      #
      # 来源：
      # https://github.com/firestar5683/StarPilot
      # branch: StarPilot
      #
      # 目标：
      # StarPilot
      # ============================================================
      - name: Fetch StarPilot Git LFS objects
        continue-on-error: true
        run: |
          echo "=== Fetching StarPilot LFS objects ==="

          git lfs fetch \
            starpilot \
            sync-starpilot

      - name: Push StarPilot Git LFS objects
        continue-on-error: true
        run: |
          echo "=== Uploading available StarPilot LFS objects ==="

          git lfs push \
            origin \
            sync-starpilot

      - name: Push StarPilot branch
        run: |
          set -e

          echo "=== Pushing StarPilot branch ==="

          git push origin \
            refs/heads/sync-starpilot:refs/heads/StarPilot \
            --force

      # ============================================================
      # 10. 同步 Amy
      #
      # 来源：
      # https://github.com/AmyJeanes/sunnypilot
      # branch: dev
      #
      # 目标：
      # Amy
      # ============================================================
      - name: Fetch Amy Git LFS objects
        continue-on-error: true
        run: |
          echo "=== Fetching Amy LFS objects ==="

          git lfs fetch \
            amy \
            sync-amy

      - name: Push Amy Git LFS objects
        continue-on-error: true
        run: |
          echo "=== Uploading available Amy LFS objects ==="

          git lfs push \
            origin \
            sync-amy

      - name: Push Amy branch
        run: |
          set -e

          echo "=== Pushing Amy branch ==="

          git push origin \
            refs/heads/sync-amy:refs/heads/Amy \
            --force

      # ============================================================
      # 11. 同步 dzid
      #
      # 来源：
      # https://github.com/dzid26/sunnypilot
      # branch: dev
      #
      # 目标：
      # dzid
      # ============================================================
      - name: Fetch dzid Git LFS objects
        continue-on-error: true
        run: |
          echo "=== Fetching dzid LFS objects ==="

          git lfs fetch \
            dzid \
            sync-dzid

      - name: Push dzid Git LFS objects
        continue-on-error: true
        run: |
          echo "=== Uploading available dzid LFS objects ==="

          git lfs push \
            origin \
            sync-dzid

      - name: Push dzid branch
        run: |
          set -e

          echo "=== Pushing dzid branch ==="

          git push origin \
            refs/heads/sync-dzid:refs/heads/dzid \
            --force

      # ============================================================
      # 12. 删除其他远程分支
      #
      # 最终仅保留：
      # master
      # Carrot
      # release
      # Dragon
      # StarPilot
      # Amy
      # dzid
      #
      # zoompilot 如果之前已经创建，也会在这里自动删除
      # ============================================================
      - name: Delete unwanted remote branches
        run: |
          set -e

          echo "=== Reading remote branches ==="

          git fetch origin --prune

          git ls-remote --heads origin \
            | awk '{print $2}' \
            | sed 's#refs/heads/##' \
            | while read -r branch; do

                case "$branch" in
                  master|Carrot|release|Dragon|StarPilot|Amy|dzid)
                    echo "Keeping branch: $branch"
                    ;;

                  *)
                    echo "Deleting branch: $branch"

                    if git push origin --delete "$branch"; then
                      echo "Deleted branch: $branch"
                    else
                      echo "Failed to delete branch: $branch"
                    fi
                    ;;
                esac
              done

      # ============================================================
      # 13. 输出最终同步结果
      # ============================================================
      - name: Show synchronization result
        if: always()
        run: |
          echo "=============================================="
          echo "=== Current remote branches ==="
          echo "=============================================="

          git ls-remote --heads origin || true

          echo ""
          echo "=============================================="
          echo "=== Git LFS environment ==="
          echo "=============================================="

          git lfs env || true

          echo ""
          echo "=============================================="
          echo "=== Local LFS object count ==="
          echo "=============================================="

          if [ -d ".git/lfs/objects" ]; then
            find .git/lfs/objects -type f | wc -l
          else
            echo "No local LFS object directory found."
          fi
